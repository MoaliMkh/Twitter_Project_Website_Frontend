import { Space, Spin } from 'antd';

const IsLoadingComponent = () => {

    return (
    <Space size="large">
        <Spin size="large" />
      </Space>
    );
}

export default IsLoadingComponent;